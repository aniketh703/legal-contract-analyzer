# HANDOFF.md
> Last updated: 20 May 2026 | Deadline: 24 May 2026

---

## Goal

Build an end-to-end **Legal Contract Analyzer** that:
1. Accepts an Indian commercial contract (PDF or TXT)
2. Segments it into individual clauses
3. Classifies each clause by type (Termination, NonCompete, etc.) and risk level (HIGH / MEDIUM / LOW)
4. Retrieves the relevant sections from the **Indian Contract Act 1872** knowledge base
5. Generates an HTML risk report with plain-English explanations and recommended actions

---

## Current State

### Pipeline — Fully Built ✅
| Stage | File | Status |
|-------|------|--------|
| Segmenter | `pipeline/segmenter.py` | Done |
| Classifier | `pipeline/classifier.py` | Done |
| Retriever | `pipeline/retriever.py` | Done |
| Generator | `pipeline/generator.py` | Done |
| Pipeline glue | `pipeline/pipeline.py` | Done |
| Flask web app | `app/main.py` + `app/templates/index.html` | Done, tested in browser |
| Evaluation | `evaluation/evaluate_classifier.py` + `evaluate_retriever.py` | Done |
| Tests | `tests/` (7 files) | **116/116 passing** (pytest + CI) |

### Data
| Resource | Count | Notes |
|----------|-------|-------|
| Raw judgment files | 330 JSON | Scraped from Indian Kanoon |
| Curated clauses | **212** | `data/processed/clauses.jsonl` |
| Verified (human-labeled) | **0** | Label Studio annotation skipped |
| ICA 1872 sections indexed | 185 | FAISS + BM25 in `knowledge_base/` |

### Evaluation Results
| Metric | Score |
|--------|-------|
| Classifier accuracy (unverified labels) | **78.8%** |
| Classifier weighted F1 | **0.783** |
| Retriever Hit@5 — BM25 only | 23.0% |
| Retriever Hit@5 — statute map + BM25 | **100%** |

### Classifier per-type F1 (latest)
| Type | F1 |
|------|----|
| Termination | 0.739 |
| Arbitration | 0.779 |
| Confidentiality | 0.667 |
| Indemnification | 0.750 |
| NonCompete | 0.875 |
| ForceMajeure | 0.667 |
| IPAssignment | 0.947 |
| LiabilityCap | **0.875** *(was 0.500)* |
| Jurisdiction | 0.772 |
| PaymentTerms | **0.780** *(was 0.222)* |
| GoverningLaw | 0.400 *(held)* |
| Renewal | 0.848 |

---

## Files In Flight

| File | What It Does | Notes |
|------|-------------|-------|
| `pipeline/curate_clauses.py` | Extracts clause candidates from raw JSON | Updated with 3 extraction patterns; LiabilityCap and Indemnification still thin |
| `app/main.py` | Flask app | Routes: `/`, `/health`, `/analyse`, `/report`, `/demo`, `/export/json`. `FLASK_DEBUG` env (default off). Artifacts: `report.html`, `analysis.json` |
| `pipeline/disclaimer.py` | Shared legal disclaimer | UI + HTML reports + JSON export |
| `pipeline/export_analysis.py` | JSON artifact builder | Used by pipeline + `/export/json` |
| `.gitignore` | venv, caches, `.env`, generated reports | `report.html` / `analysis.json` not committed |
| `evaluation/results/classifier_report.json` | Classifier precision/recall/F1 per type | Based on unverified scraped labels — re-run after annotation |
| `evaluation/results/retriever_report.json` | Retriever Hit@5 per type | Statute map layer achieves 100% — key PRD finding confirmed |
| `label_studio/import_tasks.json` | 212 tasks ready to import into Label Studio | Generated by `label_studio/prepare_import.py` — **annotation not done yet** |
| `report.html` | Last generated demo report | Served at `http://127.0.0.1:5000/report` |

---

## Things That Changed (vs. original plan)

1. **Generator is template-based, not LLM-based** — decided against Claude API to keep the project self-contained, offline, and reproducible. Templates are statute-aware and vary based on retrieved sections.

2. **Curator rewritten** — original curator capped at 50 clauses with 2 per file. Rewrote with 3 extraction patterns (numbered blocks, quoted text, keyword proximity) and per-type targeting. Reached 212 clauses.

3. **Label Studio annotation skipped** — user chose to bypass. Classifier is validated against scraped labels only (not human-verified). Accuracy numbers are indicative, not final.

4. **Retriever uses 3-layer hybrid** — original plan was FAISS only. Added statute map (guaranteed section lookup per clause type) and BM25. Key finding: BM25 alone gets 23% Hit@5; statute map + BM25 gets 100%.

5. **Flask app had `/demo` and `/report` routes added** — needed for in-browser testing since the preview tool couldn't navigate via form submission timeout.

6. **`launch.json` has 3 servers** — Flask app (5000), Label Studio (8080), HTTP report preview (8000).

7. **P0/P1 hardening (May 2026)** — Retriever KB cache + FAISS graceful degradation; pinned `requirements.txt` + GitHub Actions pytest; legal disclaimer in UI/reports/JSON; expanded `_STATUTE_MAP` (22+ types); optional PDF OCR; `GET /health`; production WSGI note in README.

---

## Failed Attempts

| Attempt | What Went Wrong | Fix |
|---------|----------------|-----|
| Scraping more Indemnification/LiabilityCap clauses | Indian Kanoon judgments discuss these types in court prose, not verbatim clause quotes — extraction patterns found nothing | Accepted thin coverage; classifier keyword rules still work without training data |
| `document.write()` to render report in preview | Caused page navigation that killed the eval context (30s timeout) | Added `/report` GET route to Flask app to serve `report.html` directly |
| `send_from_directory(ROOT, "report.html")` | Flask 3.x returned 404 even with correct path | Switched to `send_file(ROOT / "report.html")` |
| Running `curate_clauses.py` with smart quotes | Edit tool introduced Unicode curly quotes → SyntaxError | Replaced with straight ASCII quotes via Python script |

---

## Next Steps (in order)

### 1. ~~Fix classifier weak points~~ — **DONE** ✅
PaymentTerms F1: 0.222 → 0.780 | LiabilityCap F1: 0.500 → 0.875 | Overall accuracy: 65.6% → 78.8%
See `pipeline/classifier.py` — removed `required_all` for PaymentTerms, added `shall not exceed` / `consequential damages` patterns for LiabilityCap.

### 2. ~~Write README.md~~ — **DONE** ✅
Full README with architecture diagram, setup instructions, run commands, evaluation results, and key findings. See `README.md`.

### 3. ~~Polish app UX~~ — **DONE** ✅
- Visual 4-step progress list (segmenting → classifying → retrieving → generating) replaces plain spinner
- "Try a sample contract →" button on upload page fetches `/demo` with loading state
- 0-clauses error (HTTP 422) now shows titled error card with actionable tip
See `app/templates/index.html`.

### 4. ~~Final end-to-end test~~ — **DONE** ✅
Tested on both demo text contract and `app/uploads/test_contract.txt` (a real MSA).
- `test_contract.txt`: 15 clauses, 10 types, 30 KB HTML, 5 HIGH + 9 MEDIUM + 1 LOW risk labels
- ICA section references verified in report (S73, S36, S130, S74, S48, etc.)
- All pipeline checks PASS: `<html>` present, risk labels present, ICA refs present
- `report.html` updated in project root

---

## How to Run Everything

```powershell
# Activate venv
.\venv\Scripts\Activate.ps1

# Start the web app (debug off unless needed)
$env:FLASK_DEBUG = "0"
python app/main.py
# → http://127.0.0.1:5000
# Smoke: curl http://127.0.0.1:5000/health

# Run all tests
venv/Scripts/pytest tests/ -v

# Run classifier evaluation
venv/Scripts/python evaluation/evaluate_classifier.py

# Run retriever evaluation
venv/Scripts/python evaluation/evaluate_retriever.py

# Re-scrape more data
python -m pipeline.scraper --mode all --max-per-type 25 --resume

# Re-run curator after scraping
python -m pipeline.curate_clauses

# Regenerate Label Studio import
python label_studio/prepare_import.py
```

---

## Architecture (Simple)

```
User uploads contract (PDF / TXT)
        │
        ▼
  segmenter.py        → splits into clause dicts
        │
        ▼
  classifier.py       → adds clause_type + risk_level
  (keyword rules      → uses Indian Kanoon data to validate)
  + InLegalBERT
  embedding fallback)
        │
        ▼
  retriever.py        → adds retrieved_sections from ICA 1872 KB
  (statute map        → FAISS index of 185 ICA sections
  + FAISS + BM25
  + RRF fusion)
        │
        ▼
  generator.py        → builds HTML report
  (template-based,
  statute-aware)
        │
        ▼
  Flask app           → serves report at /analyse (POST)
```

### Two data sources — what each is for

| Source | Used for |
|--------|---------|
| **Indian Kanoon JSONs** | Training/evaluation data — 212 clause examples to validate the classifier |
| **Indian Contract Act 1872 PDF** | Knowledge base — 185 legal sections retrieved when explaining risk to the user |
