version: '3.8'

services:
  web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./data:/app/data
      - ./models:/app/models
      - ./knowledge_base:/app/knowledge_base
    environment:
      - FLASK_ENV=production
      - FLASK_APP=app.main:app
    restart: unless-stopped
